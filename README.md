# Predictive-Modeling-for-Agriculture

The goal of this project is to determine which single soil measurement feature (`N`, `P`, `K`, or `ph`) has the strongest predictive power for classifying crop types.  
We evaluate each feature individually using a supervised machine learning model and cross‑validation. The best feature is stored in the variable `best_predictive_feature` as a dictionary with the feature name and its evaluation score.
